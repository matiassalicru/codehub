import { Box, Typography } from '@mui/material'

export const Header = () => {
  return (
    <Box
      style={{
        display: 'flex ',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <Typography>Home</Typography>
    </Box>
  )
}
